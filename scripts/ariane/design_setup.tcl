set sdc_file ${top_proj_dir}/Benchmarks/${design_name}/${folder}/contest.sdc
set def_file ${top_proj_dir}/Benchmarks/${design_name}/${folder}/contest.def
set verilog_netlist ${top_proj_dir}/Benchmarks/${design_name}/${folder}/contest.v